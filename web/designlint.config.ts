import { defineConfig } from '@lapidist/design-lint';

// Minimal configuration for initial testing
// Rules will be configured once we understand the correct format
export default defineConfig({
  tokens: {},
  rules: {},
});
